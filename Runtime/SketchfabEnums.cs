namespace Sketchfab
{
    public enum AuthorizationType
    {
        AccessToken,
        APIToken
    }

    public enum ModelProcessingStatus
    {
        Pending,
        Processing,
        Succeeded,
        Failed
    }
}
